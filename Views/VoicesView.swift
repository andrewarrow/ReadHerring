import SwiftUI
import AVFoundation

// Keep the original VoicesView for previews
struct VoicesView: View {
    var body: some View {
        Text("Voices View")
    }
}

struct VoicesView_Previews: PreviewProvider {
    static var previews: some View {
        VoicesView()
    }
}